server:
  port: 9910

spring:
  application:
    name: target-service

logging:
  file:
    name: logs/target-service.log
  level:
    root: INFO
    com.example.targetservice: DEBUG

target-service:
  traceback-log-dir: ${TARGET_SERVICE_TRACEBACK_LOG_DIR:logs/tracebacks}
